#pragma once
// ============================================================
// VAD 模块：环境噪声估计与语音判定
// ============================================================
#include <Arduino.h>

// 空闲时喂环境帧，用最小统计法持续估计噪声并更新语音阈值
void vad_observe(int16_t peak);

// 该帧峰值是否超过语音阈值
bool vad_is_voice(int16_t peak);

// 当前语音阈值（诊断打印用）
uint16_t vad_threshold();
